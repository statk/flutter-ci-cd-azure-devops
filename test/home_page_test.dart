import 'package:flutter_test/flutter_test.dart';
import 'package:mockito/mockito.dart';

void main() {
  testWidgets('Should display current speaker when session list appears', (WidgetTester tester) async{
  });
}
